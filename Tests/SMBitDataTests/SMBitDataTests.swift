import XCTest
@testable import SMBitData

final class SMBitDataTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SMBitData().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
